# FireRescueAI 黄金演示链

> 版本: v2 ｜ 更新: 2026-08-25 ｜ 状态: **对抗多样性修复后待重新验收**
>
> 验收记录: `doc/demo-validation-2026-08-25.md`
>
> v2 机制复测: `doc/demo-validation-v2-2026-08-25.md`

> 11:45–11:56 的三轮测试只证明技术链路稳定;由于对抗 Agent 连续生成高度重复的
> “5F 影院电气短路/轰燃”特情，已撤回“对抗效果通过”结论。

## 1. 演示目标

在 5–7 分钟内证明一件事:

> AI 能基于真实建筑、预案和场景数据做出处置决策，驱动 GIS/3D 执行，经受突发特情后动态调整，并用执行回执和演练评估形成闭环。

本轮只优化这条主线。熟悉考核、多建筑泛化、额外面板和普通 UI 美化均不能阻塞它。

## 2. 固定案例

| 字段 | 固定值 |
|---|---|
| 建筑 | 乐盈广场 21 号楼 |
| 场景 ID | `478488321394200576` |
| 业务建筑 ID | `1c2d4772-831d-4c77-b88a-f9565ad589c7` |
| 初始火情 | 5F 电气起火 |
| 被困人数 | 5 人 |
| 演练 ID | 每局唯一；当前兼容值 `drill-building-21-001` |
| 特情数 | 2–3 条；至少 2 条后才评估 |

## 3. 演示前置检查

### 3.1 代码基线

```bash
npm ci
npm --prefix mcp-server ci
npm run verify
```

必须同时通过类型检查、主项目测试、MCP 构建与测试、Next.js 生产构建。

### 3.2 环境

- `NEXT_PUBLIC_X_APP_KEY` 已配置。
- `NEXT_PUBLIC_DRILL_PLANNER_APP_ID` 已配置，或明确回退到指挥官 app。
- `NEXT_PUBLIC_ADVERSARY_APP_ID` 已配置；未配置时必须明确标记回退。
- `NEXT_PUBLIC_EVALUATE_APP_ID` 已配置；未配置时 UI 必须显示“降级评估 · 规则打分”。
- BFF 与 MCP 使用同一 `MCP_APP_KEY`。
- 浏览器能加载 21 号楼场景，且 `/api/health` 与 MCP `/healthz` 都返回 HTTP 200。

### 3.3 预热

1. 提前打开页面，等待 GIS 首屏稳定。
2. 切换到对象总览，确认 21 号楼 3D 加载完成。
3. 确认浏览器命令流已连接，试跑一次楼层聚焦。
4. 演示前不清除 Service Worker/场景包缓存。

## 4. 5–7 分钟操作脚本

| 时间 | 讲解与操作 | 系统/工具行为 | 画面验收 | 降级方案 |
|---|---|---|---|---|
| 0:00–0:40 | 在态势总览介绍警情与周边力量 | Agent 查询资源，执行 `gis_fly_to` / `show_route` | GIS 聚焦案点，水源/队站/路线可见 | 路线服务失败时保留案点和资源层，明示“路线降级” |
| 0:40–1:30 | 进入对象总览，查看建筑危险性、重点部位和消防设施 | `query_building_profile` / `query_key_parts` / `query_scene_facilities` | 建筑档案与真实场景统计可见，可跳转 5F | 业务库不可用时使用已标识的演示档案；不得伪称真实查询 |
| 1:30–2:10 | 进入演练对抗，确认 5F/电气/5 人，点击“进入对抗模式” | 规划 Agent 调用 `report_decision` 生成初步部署 | 对抗舱显示初始态势、T+ 计时和部署卡 | Agent 失败时显示“使用默认部署”，演练不中断 |
| 2:10–3:10 | 等待对抗 Agent 注入第一条特情 | `inject_event` 经 MCP 下发，场景按 `location` 聚焦楼层 | 特情卡、时间轴和 3D 楼层聚焦同步出现 | 楼层无法解析时特情仍入列，显示“场景未定位”，不静默伪成功 |
| 3:10–4:00 | 讲解指挥 Agent 如何基于特情调整战法，选择“采纳调整” | `report_decision` 产生调整；场景命令返回 `cmd_id`，用 `get_scene_command_status` 确认 | 调整卡标记已采纳和响应用时，场景动作有成功/失败回执 | 命令超时时显示“未执行/已超时”，允许手工触发同一场景动作 |
| 4:00–5:10 | 第二条特情出现后选择“人工改派”，展示人在回路 | 系统记录人工决策和响应时间 | 时间轴同时存在 Agent 建议和人工最终决策 | 如特情 Agent 失败，允许使用固定备用特情，必须标记降级 |
| 5:10–6:10 | 点击“结束对抗并评估”，解释得分和改进项 | 评估 Agent 根据特情、调整、采纳与响应时间输出结构化评估 | 显示总分、维度、结论和改进措施；归档进入预案库 | Agent 不可用时用确定性规则打分，UI 显示降级来源 |
| 6:10–6:40 | 打开预案库，展示评估记录与改进措施回流 | 查询本局归档 | 建筑、评估、改进项可追溯到同一局演练 | 改进措施缺失时仅展示评估记录，不伪造项目 |

## 5. 硬性验收标准

下列条件必须全部满足，才能把当前提交标记为演示基线:

- 同一案例连续完整运行 3 次，无页面刷新、无场景卡死、无必须修改数据才能继续。
- 至少 2 条特情入列，每条都有动态调整或明确失败状态。
- 同一局特情 `type` 不重复，任意两条描述不得是轻微改写;每条必须有可观测态势增量。
- 开局由 Planner 生成部署，特情由 Adversary 生成，动态调整由 Commander 生成，不得用同一角色既出题又答题。
- Evaluator 必须收到初始部署、最终态势和完整事件—决策 timeline，并评价特情多样性与决策针对性。
- 所有场景动作有 `cmd_id` 和最终 `ok/error/timeout`，不得只显示“已下发”。
- 3D 特情联动命中正确楼层；无法聚焦时给出可见原因。
- 评估明确区分 Agent 评估与规则降级评估。
- 页面、BFF、MCP 任一端失败时有可见状态，不静默吞错或伪成功。
- `npm run verify` 通过，生产部署健康检查通过。

## 6. 演示冻结范围

在上述验收完成前，不做:

- 新增顶层业务模块。
- 扩展第二个演练建筑。
- 大规模换 UI 风格或动画库。
- 为了隐藏失败而新增静态假数据。
- 绕开 SDK/RecipeStore 直接操作 Three.js 场景对象。

## 7. 每次回归记录

```text
提交/Tag:
时间:
环境:本地 / 生产
第 1 次:通过 / 失败，失败步骤:
第 2 次:通过 / 失败，失败步骤:
第 3 次:通过 / 失败，失败步骤:
主项目测试:
MCP 测试:
生产构建:
降级路径触发情况:
结论:可作为演示基线 / 不可
```
