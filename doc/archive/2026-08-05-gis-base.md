# GIS 底座实施计划(2D 地图真实化 + 空间能力自足)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** overview 模块 2D 地图从 GisMapPlaceholder(mock SVG 线性投影)升级为天地图真实底图,消防站点位真实化,坐标转换/半径查询自足实现。

**Architecture:** Leaflet 加载天地图 WMTS 瓦片(深色大屏 CSS 滤镜);`RealGisMap` 组件替换 `GisMapPlaceholder`(复用其 sceneLog 联动逻辑,坐标由投影函数换真实经纬度);消防站点位来自 `src/api/force.ts` 的 `fetchStations`(znya 真实数据);`lib/geo-convert.ts`(BD09↔WGS84)+ `lib/geo-query.ts`(Haversine 半径过滤)自足纯函数。

**Tech Stack:** Next.js 16 / React 19 / Leaflet / 天地图 WMTS / TypeScript / vitest。

## Global Constraints

- web 独立仓库,分支 master,直接提交(项目惯例)
- vitest include 仅 `lib/**/__tests__/**`;纯函数放 `lib/`,组件不走单测
- `lib/` 下 import mock 类型用相对路径(`../src/mock/types`);`src/` 下可用 `@/` 别名(tsconfig 双别名)
- Leaflet 是浏览器库,Next SSR 下组件必须 client-only 渲染(`use client` + 动态导入/`mounted` 守卫)
- 天地图 key 从 env `NEXT_PUBLIC_TIANDITU_KEY` 读取;**本地无 key 时组件降级**(不崩溃,显示占位底图 + 点位标注 + 提示),用户后续申请填 key
- 坐标统一 **WGS84**(天地图原生 + znya 消防站坐标);`geo-convert` 提供 BD09↔WGS84 供后续接外部坐标用
- 点位第一批仅**消防站**(znya 14 站);建筑/水源/设施点位留待建筑档案,不在本 plan
- 保留 `src/mock/geo.ts`(其它组件/回退用);`GisMapPlaceholder.tsx` 删除(其交互逻辑迁入 RealGisMap)

---

### Task 1: `lib/geo-convert.ts` 坐标转换纯函数(TDD)

**Files:**
- Create: `lib/geo-convert.ts`
- Create: `lib/__tests__/geo-convert.test.ts`

**Interfaces:**
- Produces: `bd09ToWgs84(lng: number, lat: number): { lng: number; lat: number }`、`wgs84ToBd09(lng: number, lat: number): { lng: number; lat: number }`。Task 3 组件按需用;后续接外部坐标用。

- [ ] **Step 1: 写失败测试**

`lib/__tests__/geo-convert.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { bd09ToWgs84, wgs84ToBd09 } from '../geo-convert';

const EPS = 1e-6;

describe('geo-convert', () => {
  it('bd09ToWgs84 南京坐标往返后回到原点(误差 < 1e-6)', () => {
    const wgs = bd09ToWgs84(118.7969, 32.0603);
    const back = wgs84ToBd09(wgs.lng, wgs.lat);
    expect(Math.abs(back.lng - 118.7969)).toBeLessThan(EPS);
    expect(Math.abs(back.lat - 32.0603)).toBeLessThan(EPS);
  });

  it('wgs84ToBd09 已知对:北京天安门 WGS84 (116.3913,39.9073) 转 BD09 后接近 (116.3975,39.9088)', () => {
    const bd = wgs84ToBd09(116.3913, 39.9073);
    expect(Math.abs(bd.lng - 116.3975)).toBeLessThan(0.01);
    expect(Math.abs(bd.lat - 39.9088)).toBeLessThan(0.01);
  });

  it('零/极值输入不抛错', () => {
    expect(() => bd09ToWgs84(0, 0)).not.toThrow();
    expect(() => wgs84ToBd09(180, 90)).not.toThrow();
  });
});
```

> 已知对说明:百度 BD09→WGS84 的公开示例是"天安门"(BD09 116.4038,39.9151 → WGS84 116.3913,39.9073)。上面的 `wgs84ToBd09(116.3913,39.9073)` 应产出 ≈(116.4038,39.9151)。**用这个作为主断言**(更标准),把测试里的 `(116.3975,39.9088)` 修正为 `(116.4038,39.9151)`,容差 0.01。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /home/ljb/program/FireRescueAI/web && npx vitest run lib/__tests__/geo-convert.test.ts`
Expected: FAIL(`Cannot find module '../geo-convert'`)

- [ ] **Step 3: 写实现(标准公开公式)**

`lib/geo-convert.ts`:
```ts
// 坐标转换:百度 BD09 ↔ WGS84(经 GCJ02 两级纠偏)。公开标准公式,纯函数无依赖。
const PI = Math.PI;
const A = 6378245.0;           // 长半轴
const EE = 0.00669342162296594323; // 扁率平方

function outOfChina(lng: number, lat: number): boolean {
  return lng < 72.004 || lng > 137.8347 || lat < 0.8293 || lat > 55.8271;
}

function transformLat(x: number, y: number): number {
  let ret = -100.0 + 2.0 * x + 3.0 * y + 0.2 * y * y + 0.1 * x * y + 0.2 * Math.sqrt(Math.abs(x));
  ret += ((20.0 * Math.sin(6.0 * x * PI) + 20.0 * Math.sin(2.0 * x * PI)) * 2.0) / 3.0;
  ret += ((20.0 * Math.sin(y * PI) + 40.0 * Math.sin((y / 3.0) * PI)) * 2.0) / 3.0;
  ret += ((160.0 * Math.sin((y / 12.0) * PI) + 320 * Math.sin((y * PI) / 30.0)) * 2.0) / 3.0;
  return ret;
}

function transformLng(x: number, y: number): number {
  let ret = 300.0 + x + 2.0 * y + 0.1 * x * x + 0.1 * x * y + 0.1 * Math.sqrt(Math.abs(x));
  ret += ((20.0 * Math.sin(6.0 * x * PI) + 20.0 * Math.sin(2.0 * x * PI)) * 2.0) / 3.0;
  ret += ((20.0 * Math.sin(x * PI) + 40.0 * Math.sin((x / 3.0) * PI)) * 2.0) / 3.0;
  ret += ((150.0 * Math.sin((x / 12.0) * PI) + 300.0 * Math.sin((x / 30.0) * PI)) * 2.0) / 3.0;
  return ret;
}

/** WGS84 → GCJ02(国测局火星坐标)。 */
export function wgs84ToGcj02(lng: number, lat: number): { lng: number; lat: number } {
  if (outOfChina(lng, lat)) return { lng, lat };
  let dLat = transformLat(lng - 105.0, lat - 35.0);
  let dLng = transformLng(lng - 105.0, lat - 35.0);
  const radLat = (lat / 180.0) * PI;
  let magic = Math.sin(radLat);
  magic = 1 - EE * magic * magic;
  const sqrtMagic = Math.sqrt(magic);
  dLat = (dLat * 180.0) / ((A * (1 - EE)) / (magic * sqrtMagic) * PI);
  dLng = (dLng * 180.0) / ((A / sqrtMagic) * Math.cos(radLat) * PI);
  return { lng: lng + dLng, lat: lat + dLat };
}

/** GCJ02 → WGS84(近似反解,单向误差 < 1e-5,足够展示用)。 */
export function gcj02ToWgs84(lng: number, lat: number): { lng: number; lat: number } {
  const g = wgs84ToGcj02(lng, lat);
  return { lng: 2 * lng - g.lng, lat: 2 * lat - g.lat };
}

/** 百度 BD09 → WGS84。 */
export function bd09ToWgs84(lng: number, lat: number): { lng: number; lat: number } {
  const x = lng - 0.0065;
  const y = lat - 0.006;
  const z = Math.sqrt(x * x + y * y) - 0.00002 * Math.sin(y * PI);
  const theta = Math.atan2(y, x) - 0.000003 * Math.cos(x * PI);
  return gcj02ToWgs84(z * Math.cos(theta), z * Math.sin(theta));
}

/** WGS84 → 百度 BD09。 */
export function wgs84ToBd09(lng: number, lat: number): { lng: number; lat: number } {
  const g = wgs84ToGcj02(lng, lat);
  const z = Math.sqrt(g.lng * g.lng + g.lat * g.lat) + 0.00002 * Math.sin(g.lat * PI);
  const theta = Math.atan2(g.lat, g.lng) + 0.000003 * Math.cos(g.lng * PI);
  return { lng: z * Math.cos(theta) + 0.0065, lat: z * Math.sin(theta) + 0.006 };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /home/ljb/program/FireRescueAI/web && npx vitest run lib/__tests__/geo-convert.test.ts`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
cd /home/ljb/program/FireRescueAI/web
git add lib/geo-convert.ts lib/__tests__/geo-convert.test.ts
git commit -m "feat(gis): 坐标转换纯函数(BD09↔WGS84,TDD)"
```

---

### Task 2: `lib/geo-query.ts` 半径查询纯函数(TDD)

**Files:**
- Create: `lib/geo-query.ts`
- Create: `lib/__tests__/geo-query.test.ts`

**Interfaces:**
- Produces:
  - `haversineKm(lng1: number, lat1: number, lng2: number, lat2: number): number`(球面距离,公里)
  - `filterByRadius<T>(items: T[], center: { lng: number; lat: number }, radiusM: number, getLngLat: (item: T) => { lng: number; lat: number }): T[]`
  Task 3 地图点击半径查询用;后续建筑/水源接入复用。

- [ ] **Step 1: 写失败测试**

`lib/__tests__/geo-query.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { haversineKm, filterByRadius } from '../geo-query';

describe('geo-query', () => {
  it('haversineKm:南京(118.7969,32.0603)→上海(121.4737,31.2304)约 273km', () => {
    const d = haversineKm(118.7969, 32.0603, 121.4737, 31.2304);
    expect(Math.abs(d - 273)).toBeLessThan(3);
  });

  it('haversineKm:同点距离 0', () => {
    expect(haversineKm(118.7969, 32.0603, 118.7969, 32.0603)).toBe(0);
  });

  it('filterByRadius:过滤半径内的站(5000m)', () => {
    const stations = [
      { name: '城东', lng: 118.797, lat: 32.06 },
      { name: '城西', lng: 119.5, lat: 32.06 }, // ~66km 外
    ];
    const hit = filterByRadius(stations, { lng: 118.7969, lat: 32.0603 }, 5000, (s) => ({ lng: s.lng, lat: s.lat }));
    expect(hit.map((s) => s.name)).toEqual(['城东']);
  });

  it('filterByRadius:空集/全命中边界', () => {
    expect(filterByRadius([], { lng: 0, lat: 0 }, 1000, (x) => x)).toEqual([]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /home/ljb/program/FireRescueAI/web && npx vitest run lib/__tests__/geo-query.test.ts`
Expected: FAIL(`Cannot find module '../geo-query'`)

- [ ] **Step 3: 写实现**

`lib/geo-query.ts`:
```ts
// 地理查询纯函数:球面距离(Haversine)与半径过滤。无依赖。
const R = 6371; // 地球半径 km

function toRad(deg: number): number {
  return (deg * Math.PI) / 180;
}

/** 两点球面距离(公里)。 */
export function haversineKm(
  lng1: number, lat1: number,
  lng2: number, lat2: number,
): number {
  const dLat = toRad(lat2 - lat1);
  const dLng = toRad(lng2 - lng1);
  const a =
    Math.sin(dLat / 2) ** 2 +
    Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLng / 2) ** 2;
  return 2 * R * Math.asin(Math.sqrt(a));
}

/** 按半径(米)过滤,返回半径内元素。 */
export function filterByRadius<T>(
  items: T[],
  center: { lng: number; lat: number },
  radiusM: number,
  getLngLat: (item: T) => { lng: number; lat: number },
): T[] {
  const radiusKm = radiusM / 1000;
  return items.filter((item) => {
    const { lng, lat } = getLngLat(item);
    return haversineKm(center.lng, center.lat, lng, lat) <= radiusKm;
  });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /home/ljb/program/FireRescueAI/web && npx vitest run lib/__tests__/geo-query.test.ts`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
cd /home/ljb/program/FireRescueAI/web
git add lib/geo-query.ts lib/__tests__/geo-query.test.ts
git commit -m "feat(gis): 半径查询纯函数(Haversine + filterByRadius,TDD)"
```

---

### Task 3: `RealGisMap` 组件(天地图底图 + 消防站点位 + 联动)

**Files:**
- Modify: `package.json`(加 `leaflet`)
- Create: `src/components/RealGisMap.tsx`
- Modify: `src/App.tsx`(overview 挂载切换)
- Delete: `src/components/GisMapPlaceholder.tsx`
- Modify: `.env.local`(追加 `NEXT_PUBLIC_TIANDITU_KEY=`,gitignored)

**Interfaces:**
- Consumes: Task 1 `bd09ToWgs84`(备用)、Task 2 `filterByRadius`;`src/api/force.ts` 的 `fetchStations`;`src/mock/types.ts` 的 `Station`;sceneLog(`addSceneAction`/`subscribeSceneLog`);现有 `TYPE_COLORS`(GisMapPlaceholder 内)。
- Produces: overview 模块的 `RealGisMap` 组件——天地图底图 + 消防站点位 + sceneLog 联动 + 无 key 降级。

- [ ] **Step 1: 安装 leaflet + 加 key env**

```bash
cd /home/ljb/program/FireRescueAI/web
npm install leaflet
# .env.local 追加(文件已 gitignored):
# NEXT_PUBLIC_TIANDITU_KEY=
```

- [ ] **Step 2: 写 RealGisMap 组件**

`src/components/RealGisMap.tsx`(核心结构,参考现有 GisMapPlaceholder 复用联动逻辑):

```tsx
'use client';
// 态势总览 2D 地图:天地图底图(Leaflet)+ 消防站点位(znya 真实)+ sceneLog 联动。
// 无 key 时降级:不加载瓦片,显示坐标网格占位 + 点位标注 + 提示。
import { useEffect, useMemo, useRef, useState } from 'react';
import L from 'leaflet';
import 'leaflet/dist/leaflet.css';
import type { Station } from '@/mock/types';
import { fetchStations } from '@/api/force';
import { addSceneAction, subscribeSceneLog } from '@/mock/sceneLog';

const TIANDITU_KEY = process.env.NEXT_PUBLIC_TIANDITU_KEY || '';
// 天地图 vec_c(EPSG:3857,兼容 Leaflet 默认 CRS),深色大屏用 CSS 反色
const TILE_URL = `https://t{s}.tianditu.gov.cn/vec_c/wmts?SERVICE=WMTS&REQUEST=GetTile&VERSION=1.0.0&LAYER=vec&STYLE=default&TILEMATRIXSET=c&FORMAT=tiles&TILEMATRIX={z}&TILEROW={y}&TILECOL={x}&tk=${TIANDITU_KEY}`;

const TYPE_COLORS: Record<string, string> = {
  救援大队: '#f97316', 救援站: '#22d3ee', 政府专职站: '#3b82f6',
  企业专职站: '#a78bfa', 微型消防站: '#34d399',
};
const DEFAULT_CENTER: [number, number] = [32.06, 118.8];
const DEFAULT_ZOOM = 12;

export default function RealGisMap() {
  const rootRef = useRef<HTMLDivElement>(null);
  const mapRef = useRef<L.Map | null>(null);
  const markersRef = useRef<Map<string, L.Marker | L.CircleMarker>>(new Map());
  const [stations, setStations] = useState<Station[]>([]);
  const [loadState, setLoadState] = useState<'loading' | 'ok' | 'error'>('loading');
  const [selected, setSelected] = useState<Station | null>(null);

  // 初始化 Leaflet 地图(仅客户端)
  useEffect(() => {
    if (!rootRef.current || mapRef.current) return;
    const map = L.map(rootRef.current, { center: DEFAULT_CENTER, zoom: DEFAULT_ZOOM, zoomControl: false });
    mapRef.current = map;
    L.control.zoom({ position: 'bottomright' }).addTo(map);
    if (TIANDITU_KEY) {
      L.tileLayer(TILE_URL, { subdomains: ['0','1','2','3','4','5','6','7'], maxZoom: 18 })
        .addTo(map)
        .getContainer()?.classList.add('gis-dark-filter'); // 深色滤镜见 globals.css
    }
    return () => { map.remove(); mapRef.current = null; };
  }, []);

  // 加载消防站真实点位
  useEffect(() => {
    let alive = true;
    fetchStations()
      .then((st) => { if (alive) { setStations(st); setLoadState('ok'); } })
      .catch(() => { if (alive) setLoadState('error'); });
    return () => { alive = false; };
  }, []);

  // 点位 → 地图标记
  useEffect(() => {
    const map = mapRef.current;
    if (!map || !TIANDITU_KEY) return; // 无 key 降级:只显示点位列表,不渲染瓦片标记
    for (const s of stations) {
      const color = TYPE_COLORS[s.type] ?? '#22d3ee';
      const marker = L.circleMarker([s.lat, s.lng], {
        radius: 7, color, weight: 2, fillColor: color, fillOpacity: 0.35,
      })
        .addTo(map)
        .bindPopup(`<b>${s.name}</b><br/>${s.type} · 在位 ${s.personnel} 人<br/>${s.address}<br/>${s.lng}, ${s.lat}`)
        .on('click', () => handleStationClick(s));
      markersRef.current.set(s.id, marker);
    }
    return () => { markersRef.current.forEach((m) => m.remove()); markersRef.current.clear(); };
  }, [stations]);

  const handleStationClick = (s: Station) => {
    setSelected(s);
    addSceneAction({ action: 'flyTo', target: s.name, params: { lng: s.lng, lat: s.lat }, source: '面板' });
  };

  // sceneLog 联动:flyTo/addMarker → 地图定位;resetView/removeMarker → 复位
  useEffect(() => {
    const unsub = subscribeSceneLog((_list, latest) => {
      const map = mapRef.current;
      if (!map || !latest) return;
      if (latest.action === 'flyTo' || latest.action === 'addMarker') {
        const hit = stations.find((s) => latest.target?.includes(s.name));
        if (hit) {
          map.flyTo([hit.lat, hit.lng], Math.max(map.getZoom(), 14));
          const m = markersRef.current.get(hit.id);
          if (m) m.openPopup();
        }
      }
      if (latest.action === 'resetView' || latest.action === 'removeMarker') {
        setSelected(null);
      }
    });
    return () => { unsub(); };
  }, [stations]);

  return (
    <div ref={rootRef} className="relative h-full w-full overflow-hidden bg-bg-grid">
      {!TIANDITU_KEY && (
        <div className="absolute left-1/2 top-3 z-[500] -translate-x-1/2 rounded border border-line bg-bg-panel/90 px-3 py-1.5 text-[12px] text-amber-300">
          天地图 key 未配置(env NEXT_PUBLIC_TIANDITU_KEY)——显示占位底图
        </div>
      )}
      {loadState === 'error' && (
        <div className="absolute left-1/2 top-3 z-[500] -translate-x-1/2 rounded border border-line bg-bg-panel/90 px-3 py-1.5 text-[12px] text-red-300">
          消防站点位加载失败
        </div>
      )}
      {/* 无 key 降级:坐标网格 + 点位文字列表 */}
      {!TIANDITU_KEY && (
        <div className="pointer-events-none absolute inset-0 z-[400] flex items-end p-3">
          <div className="rounded-md border border-line bg-bg-panel/80 p-2 text-[11px] text-text-2">
            {stations.map((s) => (
              <div key={s.id}>{s.name} {s.lng.toFixed(4)}, {s.lat.toFixed(4)}</div>
            ))}
          </div>
        </div>
      )}
    </div>
  );
}
```

> **说明**:上面是核心结构。实际实现时——(a) 深色滤镜 CSS(`.gis-dark-filter{filter:invert(1) hue-rotate(180deg) brightness(.85) contrast(.9)}`)加到 `globals.css`;(b) `handleStationClick` 在 effect 内引用需用 `useCallback`/ref 防 stale(可把 click handler 存 ref);(c) 无 key 时 circleMarker 不应添加(上面已用 `if (!TIANDITU_KEY) return` 守卫);(d) 若 `stations.find` 闭包需最新值,用 ref 存 stations。以上实现细节以 typecheck + build 通过为准。

- [ ] **Step 3: 替换挂载 + 删旧组件 + 深色滤镜 CSS**

`src/App.tsx`:`import GisMapPlaceholder from '@/components/GisMapPlaceholder'` → `import RealGisMap from '@/components/RealGisMap'`;`{module === 'overview' ? (<GisMapPlaceholder />)` → `(<RealGisMap />)`。

删除 `src/components/GisMapPlaceholder.tsx`(其 mock 底图/建筑/水源逻辑由 RealGisMap 真实数据取代;`mock/geo.ts` 保留)。

`globals.css` 追加:
```css
/* GIS 深色地图滤镜(天地图彩色底图 → 深色指挥大屏风格) */
.gis-dark-filter { filter: invert(1) hue-rotate(180deg) brightness(.85) contrast(.9); }
```

- [ ] **Step 4: 验证**

```bash
cd /home/ljb/program/FireRescueAI/web
npm run typecheck   # 0 错
npm run build       # 成功
npx vitest run      # 全绿(原 105 + geo-convert 3 + geo-query 4 = 112)
```
走查(dev 3000):overview 模块——无 key 时显示降级占位 + 消防站点位列表;填入 key 后显示天地图底图 + 14 站点位;点站 InfoWindow + flyTo 写入 sceneLog。

- [ ] **Step 5: Commit**

```bash
cd /home/ljb/program/FireRescueAI/web
git rm src/components/GisMapPlaceholder.tsx
git add package.json package-lock.json src/components/RealGisMap.tsx src/App.tsx app/globals.css
git commit -m "feat(gis): RealGisMap 天地图底图 + 消防站真实点位 + sceneLog 联动(替换 GisMapPlaceholder)"
```

---

### Task 4: 全量验证 + 记忆 + 汇报

**Files:**
- 无代码改动(验证 + 收尾)。

- [ ] **Step 1: 全量三绿**

Run:
```bash
cd /home/ljb/program/FireRescueAI/web && npm run typecheck && npm run build && npx vitest run
```
Expected: typecheck 0 错;build 成功;vitest 112 passed(105 + 7)。

- [ ] **Step 2: 走查**

浏览器打开 `localhost:3000` overview 模块:
- 无 key 降级可见(占位底图 + 消防站列表)或填 key 后天地图底图 + 14 站点位
- 点站 → InfoWindow + 3D/面板联动(flyTo 写 sceneLog)
- overview 与 command/objects 模块切换正常;ForceResourcePanel 真实数据不受影响

- [ ] **Step 3: 记忆 + 汇报**

- 记忆:`incremental-step4-gis-base-done.md`(天地图 + Leaflet + RealGisMap + geo-convert/geo-query;key 待用户申请;组件降级;建筑/水源留后续)+ `MEMORY.md` 索引
- 汇报:改动文件、三绿、走查结果、待办(key 申请、push、znya 分支)
